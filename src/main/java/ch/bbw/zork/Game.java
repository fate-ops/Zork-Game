package ch.bbw.zork;import java.util.List;/** * Class Game - the main class of the "Zork" game. * * Author:  Michael Kolling, 1.1, March 2000 * refactoring: Rinaldo Lanza, September 2020 */public class Game {	private Parser parser;	private Room currentRoom;	private Player player;	public Game() {		parser = new Parser(System.in);		player = new Player();		createRooms();	}	private void createRooms() {		Room room1 = new Room("Room behind Door 1", 10);		Room room2 = new Room("Room behind Door 2");		room2.addItem(new Item(ItemType.KEY, "Old Key"));		Room room3 = new Room("Room behind Door 3", 15);		Room room4 = new Room("Room behind Door 4", 5);		Room room5 = new Room("Room behind Door 5");		room1.setExit("forward", room2);		room2.setExit("forward", room3);		room3.setExit("forward", room4);		room4.setExit("forward", room5);		currentRoom = room1;	}	/**	 *  Main play routine.  Loops until end of play.	 */	public void play() {		printWelcome();		// Enter the main command loop.  Here we repeatedly read commands and		// execute them until the game is over.		boolean finished = false;		while (!finished) {			Command command = parser.getCommand();			finished = processCommand(command);		}		System.out.println("Thank you for playing.  Good bye.");	}	private void printWelcome() {		System.out.println();		System.out.println("Welcome to Zork!");		System.out.println("Zork is a simple adventure game.");		System.out.println("Type 'help' if you need help.");		System.out.println();		System.out.println(currentRoom.longDescription());	}	private boolean continueToNextRoom() {		Room nextRoom = currentRoom.nextRoom("forward");		if (nextRoom == null) {			System.out.println("You reached the final door and escaped!");			return true;		}		currentRoom = nextRoom;		System.out.println(currentRoom.longDescription());		return false;	}	private void printStatus() {		String status = "Player status:\n";		status += "HP: " + player.getHp() + "\n";		status += "Coins: " + player.getCoins() + "\n";		status += player.isAlive() ? "You are alive. Keep moving." : "You are dead. Idk why you're still here.";		System.out.println(status);	}	private void searchRoom() {		boolean foundSomething = false;		int coinsFound = currentRoom.collectCoins();		if (coinsFound > 0) {			player.addCoins(coinsFound);			System.out.println("You found " + coinsFound + " coins.");			foundSomething = true;		}		List<Item> itemsFound = currentRoom.collectItems();		for (Item item : itemsFound) {			player.addItem(item);			System.out.println("You found: " + item.getName());			foundSomething = true;		}		if (!foundSomething) {			System.out.println("You searched the room but found nothing.");		}	}	private boolean processCommand(Command command) {		if (command.isUnknown()) {			System.out.println("I don't know what you mean...");			return false;		}		String commandWord = command.getCommandWord();		if (commandWord.equals("help")) {			printHelp();		} else if (commandWord.equals("go")) {			goRoom(command);		} else if (commandWord.equals("continue")) {			return continueToNextRoom();		} else if (commandWord.equals("status")) {			printStatus();		} else if (commandWord.equals("search")) {			searchRoom();		} else if (commandWord.equals("inventory")) {			System.out.println(player.inventoryDescription());		} else if (commandWord.equals("quit")) {			if (command.hasSecondWord()) {				System.out.println("Quit what?");			} else {				return true; // signal that we want to quit			}		}		return false;	}	private void printHelp() {		System.out.println("You are lost. You are alone. You wander");		System.out.println("around at Monash Uni, Peninsula Campus.");		System.out.println();		System.out.println("Your command words are:");		System.out.println(parser.showCommands());	}	private void goRoom(Command command) {		if (!command.hasSecondWord()) {			System.out.println("Go where?");		} else {			String direction = command.getSecondWord();			// Try to leave current room.			Room nextRoom = currentRoom.nextRoom(direction);			if (nextRoom == null)				System.out.println("There is no door!");			else {				currentRoom = nextRoom;				System.out.println(currentRoom.longDescription());			}		}	}}